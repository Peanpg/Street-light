// Compatibility placeholder for repositories that accidentally uploaded the
// survey component at the repository root. The actual component is in
// app/survey-app.tsx. Keeping this file harmless lets a full-file upload
// replace the misplaced version without a separate GitHub deletion step.
export {};
